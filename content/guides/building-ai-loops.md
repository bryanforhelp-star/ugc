---
title: "5 ai loops you can actually use"
description: "Five loops that replace one-off prompts, plus a YouTube walkthrough for going from zero to a working loop."
summary: "Pick one of five loops, then watch the setup walkthrough to build it. A loop runs, learns, and improves. A prompt just stops."
category: guide
topics:
  - loops
tools: []
cover: "prompting is not the skill anymore. building loops is."
commentKeyword: LOOP
pillars:
  - how-to
  - problem-solution
tags:
  - ai
  - loops
published: true
date: 2026-07-04
updated: 2026-07-04
---

you commented for the guide. here it is.

**prompt:** get an answer.

**workflow:** complete a task.

**loop:** build a system that improves.

a prompt is: input → output → stop.

a loop is: input → action → evaluation → next action → repeat until the goal is closer.

the difference: **the output of one cycle changes how the next cycle runs.** you're not asking the same question again. you built something that learns.

below are 5 loops you can actually use. pick the one closest to your life. then watch the setup walkthrough to build it.

---

## 5 ai loops you can actually use

### 1. lead generation loop

instead of:

> find me leads.

build a loop that:

- finds companies that match your criteria
- researches their business
- generates personalized outreach
- tracks who responds
- learns which messages perform best
- improves future outreach

**goal:** better leads over time.

---

### 2. research loop

instead of:

> what's happening in ai?

build a loop that:

- pulls articles from multiple sources
- identifies recurring themes
- filters out noise
- summarizes key insights
- connects them to your interests
- refines what it looks for next

**goal:** better signal, less noise.

---

### 3. learning loop

instead of:

> teach me coding.

build a loop that:

- explains a concept
- tests your understanding
- identifies gaps
- re-explains difficult topics
- generates exercises
- adjusts based on what you struggle with

**goal:** learn faster.

want more on this one: [the ai learning loop guide](/guides/ai-learning-loop).

---

### 4. email loop

instead of:

> help me answer this email.

build a loop that:

- categorizes incoming emails
- identifies priority items
- drafts responses in your voice
- learns from your edits
- improves future drafts

**goal:** spend less time in your inbox.

---

### 5. decision-making loop

instead of:

> should i buy this?

build a loop that:

- researches options
- compares reviews
- scores based on your priorities
- identifies tradeoffs
- makes a recommendation
- learns from your eventual decision

**goal:** make better decisions over time.

---

## go from zero to a working loop

this page is the **what**. the examples above. the goals. the difference between a prompt and a loop.

to build one, you need the **how**.

i build my loops in [hermes agent](https://hermes-agent.nousresearch.com/docs/). i don't have a clean claude-only version to hand you, so i recorded a walkthrough instead.

**watch:** [how to set up your first ai loop →](https://www.youtube.com/@withkyndall)

the video covers:

1. pick a loop from the list above
2. set a goal (one sentence, not a question)
3. wire up the steps so the system runs, evaluates, and updates itself
4. run it once, check what changed, run it again

that's zero → loop. no copy-pasting prompts back and forth. an actual system that gets better each time it runs.

---

## what to do next

1. pick one loop from the list.
2. watch the [setup walkthrough](https://www.youtube.com/@withkyndall).
3. build it.
4. run it twice. if the second run is smarter than the first, you have a loop.

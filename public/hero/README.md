# Hero media

- `kyndall-poster.jpg` — portrait poster (fallback)
- `kyndall.mp4` — looping hero video

Replace these files to update the homepage portrait. Keep the same filenames or update `HomePortrait.tsx`.

# Fonts

Homepage fonts (extracted from your design):

- `Bootzy-TM.woff2` — headlines
- `NewPixel.woff2` — body / serif text
- `NewPixelSquare.woff2` — facts list

To swap the headline font, replace `Bootzy-TM.woff2` and update the `@font-face` in `src/app/home.css`.
